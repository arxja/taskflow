{
  "name": "taskflow",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.9.14",
    "@biomejs/biome": "^1.7.3"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "biome format --write .",
    "check": "biome check .",
    "clean": "turbo clean"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
